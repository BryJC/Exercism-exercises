from collections import defaultdict

def word_count(text):
    txt_split = text.split()
    d = defaultdict(int)
    for i in L:
        d[i] += 1
    d
        
        
    count_dic = {}
    return count_dic
