from datetime import date

def add_gigasecond(date):
    # number of days in 1 Gigasecond
    x = (10.0**9)/(24.0*60*60)
    
    #attempt
    date_in_days = (year*
    
    date + x = final_date

    return final_date
